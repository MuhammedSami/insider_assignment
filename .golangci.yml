version: "3"

run:
  timeout: 3m
  tests: true

linters:
  enable:
    - govet
    - gofmt
    - goimports
    - errcheck
    - staticcheck
    - revive
    - gosimple
    - ineffassign
    - unused
    - gocyclo

issues:
  exclude-use-default: false
  max-issues-per-linter: 0
  max-same-issues: 0